{
  "name": "eva-desktop",
  "version": "0.2.0",
  "description": "Eva — ANIMA Desktop Client (Tauri v2)",
  "private": true,
  "scripts": {
    "dev": "npx tauri dev",
    "build": "npx tauri build",
    "tauri": "npx tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/cli": "^2.10.1"
  }
}
